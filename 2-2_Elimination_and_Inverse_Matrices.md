# 2.2 — 소거 행렬과 역행렬: 행동을 기록하고 되돌리기 (Elimination and Inverse Matrices)

> **선생님의 한마디**: "지난 시간에 손으로 열심히 깎았던 '소거' 과정을 기억하시나요? 오늘은 그 '행동' 자체를 하나의 행렬로 만들어볼 거예요. 행동을 행렬로 기록하면, 나중에 마법처럼 그 행동을 취소(역행렬)할 수도 있답니다!"

---

## 목차

- [1. 기초 재료: 아무것도 안 하는 단위 행렬 (I)](#identity-matrix)
- [2. 행동을 행렬로 기록하기: 소거 행렬 (E)](#elimination-matrix)
  - [2.1 실전 예시: 소거 행렬로 0 만들기](#example-zero)
- [3. 마법의 되돌리기 버튼: 역행렬 (Inverse Matrix)](#inverse-matrix)
  - [3.1 소거 행렬의 역행렬: "취소" 의 수치적 의미](#inverse-meaning)
- [4. 역행렬 구하는 표준 절차: 가우스-조던 법 (실전 연습)](#gauss-jordan)
- [5. 주의사항: 순서가 뒤집힌다!](#order-reverse)
- [요약](#summary)

---

<br>

<a id="identity-matrix"></a>
## 1. 기초 재료: 아무것도 안 하는 단위 행렬 ($I$)

행동을 기록하기 전에, 아무런 행동도 하지 않는 '중립' 상태의 행렬을 알아야 합니다. 그것이 바로 **단위 행렬 ($I$, Identity Matrix)** 입니다.

- **모양**: 대각선은 모두 **1** 이고, 나머지는 모두 **0** 인 행렬입니다.
- **의미**: 숫자계의 **'1'** 과 같습니다. 어떤 행렬 $A$ 에 $I$ 를 곱해도 결과는 그대로 $A$ 입니다.

$$
I = \begin{bmatrix} 1 & 0 \\\\ 0 & 1 \end{bmatrix}, \quad AI = A, \quad IA = A
$$

---

<br>

<a id="elimination-matrix"></a>
## 2. 행동을 행렬로 기록하기: 소거 행렬 ($E$)

우리는 지금까지 "2행에서 1행의 3배를 빼라" 고 말로 명령했습니다. 컴퓨터에게는 이 명령을 **'행렬 곱셈'** 형태로 전달해야 합니다.

<a id="example-zero"></a>
### 2.1 실전 예시: 소거 행렬로 $0$ 만들기

다음과 같은 행렬 $A$ 가 있다고 합시다. 2행 1열의 **'3'** 을 없애고 싶습니다.

$$
A = \begin{bmatrix} 1 & 2 \\\\ 3 & 8 \end{bmatrix}
$$

우리의 명령은 "2행에서 1행의 **3** 배를 빼라" 입니다. 이 명령을 담은 소거 행렬 $E\_{21}$ 은 다음과 같습니다.

$$
E\_{21} = \begin{bmatrix} 1 & 0 \\\\ -3 & 1 \end{bmatrix}
$$

실제로 $E\_{21}A$ 를 계산해 볼까요?

$$
\begin{bmatrix} 1 & 0 \\\\ -3 & 1 \end{bmatrix} \begin{bmatrix} 1 & 2 \\\\ 3 & 8 \end{bmatrix} = \begin{bmatrix} (1)(1)+(0)(3) & (1)(2)+(0)(8) \\\\ (-3)(1)+(1)(3) & (-3)(2)+(1)(8) \end{bmatrix} = \begin{bmatrix} 1 & 2 \\\\ 0 & 2 \end{bmatrix} = U
$$

보이시나요? 2행 1열 자리가 정확히 **0** 이 되었습니다!

---

<br>

<a id="inverse-matrix"></a>
## 3. 마법의 되돌리기 버튼: 역행렬 (Inverse Matrix)

어떤 행렬 $A$ 가 공간을 변형시켰다면, 그 변형을 **완벽하게 취소** 하고 원래대로 돌려놓는 행렬이 바로 **역행렬 ($A^{-1}$)** 입니다.

<a id="inverse-meaning"></a>
### 3.1 소거 행렬의 역행렬: "취소" 의 수치적 의미

위에서 우리는 "1행의 3배를 **뺐습니다.**" ( $-3$ )
이 행동을 취소하려면 어떻게 해야 할까요? 당연히 "1행의 3배를 다시 **더해주면**" 됩니다. ( $+3$ )

따라서 다음 소거 행렬 $E\_{21}$ 의 역행렬 $E\_{21}^{-1}$ 은 단순히 부호만 바꾼 행렬이 됩니다.

$$
E\_{21} = \begin{bmatrix} 1 & 0 \\\\ -3 & 1 \end{bmatrix} \implies E\_{21}^{-1} = \begin{bmatrix} 1 & 0 \\\\ 3 & 1 \end{bmatrix}
$$

실제로 두 행렬을 곱하면 아무 일도 없었던 것처럼 **단위 행렬 ($I$)** 이 됩니다.

$$
\begin{bmatrix} 1 & 0 \\\\ 3 & 1 \end{bmatrix} \begin{bmatrix} 1 & 0 \\\\ -3 & 1 \end{bmatrix} = \begin{bmatrix} 1 & 0 \\\\ 0 & 1 \end{bmatrix} = I
$$

---

<br>

<a id="gauss-jordan"></a>
## 4. 역행렬 구하는 표준 절차: 가우스-조던 법 (실전 연습)

이제 조금 더 복잡한 행렬 $A$ 의 역행렬을 찾아봅시다.

$$
A = \begin{bmatrix} 1 & 3 \\\\ 2 & 7 \end{bmatrix}
$$

**Step 1: 확장 행렬 만들기 $[A \mid I]$**
오른쪽에 단위 행렬을 붙입니다.

$$
\begin{bmatrix} 1 & 3 & \mid & 1 & 0 \\\\ 2 & 7 & \mid & 0 & 1 \end{bmatrix}
$$

**Step 2: 왼쪽(A) 을 소거하여 단위 행렬(I) 로 만들기**
- 2행 소거: (2행) $-$ (1행 $\times 2$)

$$
\begin{bmatrix} 1 & 3 & \mid & 1 & 0 \\\\ 0 & 1 & \mid & -2 & 1 \end{bmatrix}
$$

- 1행 소거: (1행) $-$ (2행 $\times 3$)

$$
\begin{bmatrix} 1 & 0 & \mid & 7 & -3 \\\\ 0 & 1 & \mid & -2 & 1 \end{bmatrix}
$$

**Step 3: 오른쪽 결과 확인**
왼쪽이 $I$ 가 되었을 때, 오른쪽에 남은 행렬이 바로 **역행렬 $A^{-1}$** 입니다.

$$
A^{-1} = \begin{bmatrix} 7 & -3 \\\\ -2 & 1 \end{bmatrix}
$$

---

<br>

<a id="order-reverse"></a>
## 5. 주의사항 1: 역행렬이 없는 경우

**모든 행렬에 역행렬이 존재하는 것은 아닙니다!** 가우스-조던 법을 쓰다가 왼쪽이 단위 행렬 $I$ 로 변환되지 않는다면 역행렬이 없는 것입니다. 이런 행렬을 **특이 행렬(Singular Matrix)** 이라고 합니다.

**역행렬이 없는 경우의 예**:

$$
A = \begin{bmatrix} 1 & 2 \\\\ 2 & 4 \end{bmatrix}
$$

2행이 1행의 2배입니다. 소거하면 2행 전체가 $[0, 0]$ 이 되어, 왼쪽을 $I$ 로 만들 방법이 없습니다. 이 행렬의 두 열이 같은 방향을 가리켜 평면 전체를 커버하지 못하기 때문입니다.

## 6. 주의사항 2: 순서가 뒤집힌다!

양말($S$) 을 신고 신발($B$) 을 신는 행동을 행렬 곱셈 $BS$ 라고 합시다.
이것을 취소(역행렬) 하려면? 당연히 **신발($B^{-1}$) 을 먼저 벗고, 양말($S^{-1}$) 을 나중에 벗어야 합니다.**

$$
(AB)^{-1} = B^{-1} A^{-1}
$$

이처럼 행렬의 곱을 역행렬 취하면 **순서가 반대로 바뀐다** 는 사실을 꼭 기억하세요!

---

<br>

<a id="summary"></a>
## 요약

| 용어 | 쉬운 비유 | 수학적 정의 |
|:-----|:-----|:-----|
| **단위 행렬 (I)** | 아무것도 안 하기 (숫자 1) | $AI = A$ |
| **소거 행렬 (E)** | "몇 행에서 몇 배 빼기" 기록 | $EA = U$ |
| **역행렬 (A^-1)** | "되돌리기" 버튼 (취소) | $AA^{-1} = I$ |
| **가우스-조던 법** | $[A \mid I] \rightarrow [I \mid A^{-1}]$ | 역행렬 구하는 알고리즘 |

---

<br>

> **다음 시간에는**: 소거 전의 기록($L$) 과 소거 후의 결과($U$) 를 완벽하게 보관하는 **$A = LU$ 분해** 에 대해 알아보겠습니다. 수고하셨습니다! 👋🏻
